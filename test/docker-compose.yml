services:
  stellar-test:
    image: pinetwork/pi-node-docker:organization_mainnet-v1.0-p19.9
    container_name: stellar-test
    ports:
      - "8000:8000"
    volumes:
      - /Users/pierre/stellar-test:/opt/stellar
    environment:
      - PGPASS=stellar
    command: ["--mainnet"]
    restart: "no"

